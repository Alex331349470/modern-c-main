#include <stdio.h>

int main(void) {
  int num;

  printf("Please enter a num: ");
  scanf("%d", &num);

  printf("The entered number is %d\n", num);
  printf("this is how to write with the neovim editor, and it's awesome for "
         "coding the C project, this is wonderful!\n");

  return 0;
}
